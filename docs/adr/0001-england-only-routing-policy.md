# Use an England-only routing policy for the first version

The first version will route only in England. Access rights differ materially across the UK—especially Scotland and Northern Ireland—so treating UK expansion as a data-only change would produce incorrect route classifications; any later expansion must introduce a jurisdiction-specific access policy.
